import React from 'react';
import styled from 'styled-components';

const Styled = styled.div``;

const ItemComments = () => {
    return <Styled>ItemComments</Styled>;
};

export default ItemComments;
