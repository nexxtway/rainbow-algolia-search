import React from 'react';
import styled from 'styled-components';

const Styled = styled.div``;

const SearchResults = () => {
    return <Styled>SearchResults</Styled>;
};

export default SearchResults;
