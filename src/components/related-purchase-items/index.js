import React from 'react';
import styled from 'styled-components';

const Styled = styled.div``;

const RelatedPurchaseItems = () => {
    return <Styled>RelatedPurchaseItems</Styled>;
};

export default RelatedPurchaseItems;
