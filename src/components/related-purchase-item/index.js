import React from 'react';
import styled from 'styled-components';

const Styled = styled.div``;

const RelatedPurchaseItem = () => {
    return <Styled>RelatedPurchaseItem</Styled>;
};

export default RelatedPurchaseItem;
